{
  "name": "motoledger",
  "version": "0.1.0",
  "private": true,
  "description": "Motorcycle asset care app with a local Node auth backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
